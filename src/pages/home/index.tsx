// @flow
import * as React from 'react';
import { NavBar } from '../../components/partials/NavBar';
type Props = {
    
};

const Home = (props: Props) => {
    return (
        <div>
            <NavBar/>

            <div>
                Home Page
            </div>
        </div>
    );
};

export default Home